# Profile: PJ-00 Chronicles

> 這是 `PJ-00_Chronicles` 的專案層 profile。全域原則請先讀 `D:\Project\PJ-00_Chronicles\Core_Rules\CORE_RULES.md`。

## 專案定位

- `PJ-00_Chronicles` 是紀錄與收口工作區，不是新的法典本體。
- 真正需要跨 repo 的治理、狀態與協定，仍以 `PJ-14_Twin-Sync` 與其對應文件為準。
- `chat/` 只承接 session log，不承接全域規則本體。

## Canonical Read Order

1. `D:\Project\PJ-00_Chronicles\Core_Rules\CORE_RULES.md`
2. `D:\Project\PJ-00_Chronicles\Core_Rules\profiles\pj00-chronicles.md`
3. `D:\Project\PJ-00_Chronicles\AGENTS.md`
4. `D:\Project\PJ-00_Chronicles\AI_Workflow_OnePager.md`
5. `D:\Project\PJ-00_Chronicles\NEXT_STEPS.md`
6. 最新的 `D:\Project\PJ-00_Chronicles\chat\` session log

## 工作邊界

- 這裡優先做日誌、收口與最小可驗證修正。
- 若要新增 workflow 規則，先確認是否已經影響到核心原則；若有，就回寫 `CORE_RULES.md`。
- session log 只在 `chat/`，不要寫到其它 root 入口。
- `Spectra` 僅在明確有用時採用；若不用，保留簡化收口流程即可。

## 禁止事項

- 不要把 `PJ-00_Chronicles` 當作新的治理法典本體。
- 不要把 session log、task packet 或 runtime payload 塞進專案 profile。
- 不要把專案 profile 擴寫成第二份核心規則。
