# PJ-00 Chronicles Wrapper

本檔是 `PJ-00_Chronicles` 的 repo-local 入口。真正的核心規則不在這裡，而是在：

- `D:\Project\PJ-00_Chronicles\Core_Rules\CORE_RULES.md`
- `D:\Project\PJ-00_Chronicles\Core_Rules\profiles\pj00-chronicles.md`

若本檔與核心規則衝突，以核心規則為準。

## 這個 repo 的角色

- `PJ-00_Chronicles` 是紀錄與收口工作區，不是新的法典本體。
- 真正需要跨 repo 的治理、狀態與協定，仍以 `PJ-14_Twin-Sync` 與其對應文件為準。
- `chat/` 只承接 session log，不承接全域規則本體。

## 第一批閱讀

1. `D:\Project\PJ-00_Chronicles\Core_Rules\CORE_RULES.md`
2. `D:\Project\PJ-00_Chronicles\Core_Rules\profiles\pj00-chronicles.md`
3. `AI_Workflow_OnePager.md`
4. `NEXT_STEPS.md`
5. 最新的 `chat\` session log

## Repo-local facts

- 這裡保留的是記錄、收口與日誌入口，不是全域法典。
- 若需要 workflow 追蹤，優先先記錄現況，再做最小可驗證修正。
- Git 操作維持 path-scoped，不使用 `git add .`、`git add -A` 或 `git commit -a`。
